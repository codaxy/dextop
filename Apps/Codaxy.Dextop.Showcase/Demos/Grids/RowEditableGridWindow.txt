Row Editable Grid Window
========================

This demo shows how to edit grid data using the RowEditor plugin.

autoRevert - revert any change rejected by the server
autoSync - sync with the server on every change

**Note**: *Neo* is reserved name and it will be rejected by the server.